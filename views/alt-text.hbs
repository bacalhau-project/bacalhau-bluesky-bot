<main>

    <section>

        {{#if LVM_TEXT}}
        <article>
            <h2>Large Vision Model Description</h2>
            {{{LVM_TEXT}}}
        </article>
        {{/if}}

        {{#if OCR_TEXT}}
        <article>
            <h2>Text extracted via OCR</h2>
            {{{OCR_TEXT}}}
        </article>
        {{/if}}

    </section>

    <section>
        <img src="{{IMAGE_URL}}" />
    </section>

</main>